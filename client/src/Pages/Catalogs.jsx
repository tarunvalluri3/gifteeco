import StubPage from "../components/StubPage";

const Catalogs = () => (
  <StubPage
    eyebrow="Catalogs"
    title="Browsable catalogs, coming soon."
    description="Curated collections by occasion, budget, and industry — built for easy bulk ordering."
  />
);

export default Catalogs;
